#!/usr/bin/env bash
# 'getent hosts' - used by gravity.sh

case "$1" in
    hosts)
        nslookup "$2" && exit 0
        exit 1
    ;;
esac

echo "'getent $*' - polyfill not implemented" >&2
exit 1
